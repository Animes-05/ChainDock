DATABASE_URL=postgres://user:password@localhost:5432/casevault
JWT_SECRET=change-me-to-a-long-random-string
PORT=3000
LEDGER_SERVICE_URL=http://127.0.0.1:3001
# Default logical org tag for ledger writes from this instance (POLICE/COURT/FORENSICS).
# Empty = untagged (back-compat). On Railway, set per environment as needed.
#DEFAULT_ORG=
